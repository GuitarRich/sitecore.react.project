// //@tscheck
// var React = require("react");
// var Placeholder = require("./../Test/Placeholders2");

// var HeaderTop = require("./../Sublayouts/HeaderTop");
// var Navbar = require("./../Sublayouts/Navbar");

var Layout = React.createClass({
    render() {
        return (
            <div id="main-container">
                <header class="header-static">
                    <Placeholder placeholderKey={'header-top'}>
                        <HeaderTop />
                    </Placeholder>
                    <Placeholder placeholderKey={'navbar'}>
                        <Navbar />
                    </Placeholder>
                </header>

                <main role="main">
                    <Placeholder placeholderKey={'main'}>
                        {this.props.children}
                    </Placeholder>
                </main>

                <footer class="bg-dark">
                    <Placeholder placeholderKey={'footer'}>
                        <p>todo: add footer</p>
                    </Placeholder>
                </footer>
            </div>
        );
    }
});

//module.exports = Layout;